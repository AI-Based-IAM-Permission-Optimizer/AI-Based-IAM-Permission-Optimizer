{
  "name": "iam-permission-optimizer-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Backend API for the AI-Based IAM Permission Optimizer.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.1136.0",
    "@vendia/serverless-express": "^4.12.6",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.0.0"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}
